// ================================================================
// ========================  Use Component  =======================
// ================================================================

// Lib

// include

// start
const TEST = async () => {
  console.log('=== Hello : npm-components-be ===')
  /**
   *  Declare function from `components` here
   *  - 📌 Only from `other.ts`
   *  - 🤯 Except from `dynamoDB.ts` because need to set aws-config (key, secret_key, region ...other)
   */
}

TEST()
